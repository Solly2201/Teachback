fastapi>=0.110
uvicorn>=0.27
sqlalchemy>=2.0
pydantic>=2.5
sentence-transformers>=2.5
hmmlearn>=0.3
scipy>=1.11
numpy>=1.26
pandas>=2.1
joblib>=1.3
pytest>=8.0
httpx>=0.26
# Lecture PDF ingestion. pdfplumber is the primary extractor because it
# reports per-word bounding boxes and font size, which is what tells a running
# footer apart from a slide title; pypdf stays as a text-only fallback when a
# file cannot be opened with pdfplumber.
pdfplumber>=0.11
pypdf>=4.0
